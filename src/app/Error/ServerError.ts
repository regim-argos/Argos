// import FFError from './FFError';

// export default class ServerError extends FFError {
//   constructor() {
//     const message = `Something went wrong, but don't worry, our developers are already working on this`;
//     super(message, 'serverError', 500);
//   }
// }
