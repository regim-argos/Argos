// import FFError from "./FFError";

// export default class DuplicateFields extends FFError {
//   constructor(fields: string) {
//     super(`The ${fields} already exist`,'duplicateFields',400);
//   }
// }
